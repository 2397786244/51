C51 COMPILER V9.54   CLOCK                                                                 04/10/2022 19:02:40 PAGE 1   


C51 COMPILER V9.54, COMPILATION OF MODULE CLOCK
OBJECT MODULE PLACED IN .\Objects\clock.obj
COMPILER INVOKED BY: D:\Keil5\C51\BIN\C51.EXE clock.c OPTIMIZE(8,SPEED) BROWSE DEBUG OBJECTEXTEND PRINT(.\Listings\clock
                    -.lst) OBJECT(.\Objects\clock.obj)

line level    source

   1          #include <reg52.h>
   2          #include <stdio.h>
   3          //DS1302写寄存器 起始地址
   4          #define REG_W_START 0x80
   5          //DS1302 读寄存器 起始地址
   6          #define REG_R_START 0x81
   7          
   8          /*
   9          使用LCD1602显示当前的时间
  10          按下按键S2，显示当前的年月日信息(星期）。
  11          使用ds1302 RAM储存信息。
  12          按下S3，发送时间到串口。
  13          */
  14          
  15          //DS1302三个引脚使用P1.0 1.1 1.2，用杜邦线提前连接
  16          sbit SCLK = P1^0;
  17          sbit IO = P1^1;
  18          sbit CS = P1^2;
  19          
  20          //LCD 1602 引脚
  21          sbit LCDEN = P3^4;
  22          sbit RS = P3^5; //数据，命令选择
  23          sbit RW = P3^6; //数据读写。
  24          
  25          //按键
  26          sbit S2 = P3^0;
  27          sbit S3 = P3^1;
  28          
  29          typedef unsigned char uchar;
  30          typedef unsigned int uint;
  31          
  32          //LCD1602 函数部分 命令发送，数据发送，指针位置设置，显示屏初始化
  33          uchar TIME_AR[7] = {
  34          20,50,5,14,7,1,22
  35          };
  36          
  37          //设置LCD显示状态
  38          enum STATES {CLOCK,Y_M_D};
  39          static enum STATES S = CLOCK;
  40          
  41          void sendTimeToSeries();
  42          //延时函数
  43          void delay(uchar time){
  44   1              uint i,j;
  45   1              for(i = 0;i<114;i++){
  46   2                      for(j = 0;j < time;j++);
  47   2              }
  48   1      }
  49          //判断是否空闲
  50          void readStates(){
  51   1              uchar flag = 0;
  52   1              P0 = 0xff; //把P0寄存器（即D0-D7）拉高。
  53   1              RS = 0;  //指令选择
  54   1              RW = 1;  //读选择
C51 COMPILER V9.54   CLOCK                                                                 04/10/2022 19:02:40 PAGE 2   

  55   1              do{
  56   2                      LCDEN = 1;  //使能
  57   2                      flag = P0;  //把数据口赋值给flag，判断flag第八位(D7)是否为0，为0则空闲退出循环。
  58   2                      LCDEN = 0;
  59   2              }while(flag & 0x80);
  60   1      }
  61          
  62          //发送命令
  63          void SendCmd(uchar cmd){
  64   1               //每次发出指令前判断器件是否空闲
  65   1              readStates();
  66   1              RS = 0;
  67   1              RW = 0;
  68   1              P0 = cmd;
  69   1              LCDEN = 1;
  70   1              LCDEN = 0;//指令发送完之后拉低。两次使能信号改变语句之间间隔在时序规定的最大值之内
  71   1      }
  72          
  73          //数据发送
  74          void SendData(uchar dat){
  75   1              readStates();
  76   1              RS = 1;  //选择数据
  77   1              RW = 0;  //写
  78   1              P0 = dat; 
  79   1              LCDEN = 1;
  80   1              LCDEN = 0;//使能保持150ns
  81   1      }
  82          
  83          //指针位置的设置，调用发送命令函数
  84          int setPointer(uchar x,uchar y){
  85   1              switch(x){
  86   2                      case 1: SendCmd(0x80 | y);break;
  87   2                      case 2: SendCmd(0x80 | 0x40 | y);break;
  88   2              }
  89   1              return 1;
  90   1      }
  91          
  92          //显示屏初始化
  93          void lcdInit(){
  94   1              SendCmd(0x38);
  95   1              SendCmd(0x0c); 
  96   1              SendCmd(0x01); //清屏
  97   1              SendCmd(0x06); //输入一个之后指针往后移动。
  98   1      }
  99          
 100          //发送字符串到显示屏。调用SendData函数
 101          void SendString(uchar * str){
 102   1              while(*str != '\0'){
 103   2                      SendData(*str++);
 104   2              }
 105   1      }
 106          
 107          //DS1302部分 器件初始化，时间获取
 108          
 109          //写入一字节
 110          void ds1302SendByte(uchar dat){
 111   1              uint i;
 112   1              for(i = 0;i<8;i++){
 113   2                      SCLK = 0;
 114   2                      IO = dat & 0x01;
 115   2                      SCLK = 1;
 116   2                      dat >>= 1;
C51 COMPILER V9.54   CLOCK                                                                 04/10/2022 19:02:40 PAGE 3   

 117   2              }
 118   1      }
 119          
 120          //读一字节
 121          uchar ds1302ReadByte(){
 122   1              uint i;
 123   1              uchar dat;
 124   1              SCLK = 1;
 125   1              for(i = 0;i<8;i++){
 126   2                      SCLK = 0;
 127   2                      dat >>= 1;              
 128   2                      if(IO){
 129   3                              dat |= 0x80;
 130   3                      }
 131   2                      SCLK = 1;       
 132   2              }
 133   1              return dat;
 134   1      }
 135          
 136          //发送数据 DS1302发送数据，先发送命令，再发送数据
 137          void DS1302Send(uchar cmd,uchar dat){
 138   1              CS = 0;
 139   1              SCLK = 0;
 140   1              CS = 1;
 141   1              ds1302SendByte(cmd);
 142   1              ds1302SendByte(dat);
 143   1              SCLK = 1; //拉高时钟线
 144   1              CS = 0;
 145   1      }
 146          
 147          //BCD转换为十进制码
 148          uchar bcd2d(uchar bcd){
 149   1              uchar c1,c2;
 150   1              c1 = bcd / 16;
 151   1              c2 = bcd % 16;
 152   1              return c1 * 10 + c2;
 153   1      }
 154          
 155          //十进制转换为BCD
 156          uchar d2bcd(uchar d){
 157   1              uchar c1,c2;
 158   1              c1 = d / 10;
 159   1              c2 = d % 10;
 160   1              return c1 * 16 + c2; 
 161   1      }
 162          
 163          //读取数据 ,DS1302读取数据，先发送命令，再读取数据
 164          uchar DS1302Read(uchar cmd){
 165   1              uchar dat;
 166   1              CS = 0;
 167   1              SCLK = 0;
 168   1              CS = 1;
 169   1              ds1302SendByte(cmd);
 170   1              dat = ds1302ReadByte();
 171   1              SCLK = 1;
 172   1              CS = 0;
 173   1              return dat;
 174   1      }
 175          
 176          //DS1302写保护,寄存器位于0x8e
 177          //设置写保护，1打开。
 178          void writeOpen(uchar flag){
C51 COMPILER V9.54   CLOCK                                                                 04/10/2022 19:02:40 PAGE 4   

 179   1              DS1302Send(0x8e,flag);
 180   1      }
 181          
 182          //器件初始化。设置日历的初始值。
 183          void DS1302Init(){
 184   1              uint i;
 185   1              uchar start_addr = REG_W_START;
 186   1              writeOpen(0);
 187   1              for(i = 0;i<7;i++)
 188   1              {
 189   2                      DS1302Send(start_addr,d2bcd(TIME_AR[i]));
 190   2                      start_addr+=2;
 191   2              }
 192   1              writeOpen(1);
 193   1      }
 194          
 195          //寄存器时间读取,结果储存在数组TIME_AR中。
 196          void timeGetAndStore()
 197          {
 198   1              uint i;
 199   1              uchar start_addr = REG_R_START;
 200   1              for(i = 0;i<7;i++){
 201   2                      TIME_AR[i] = bcd2d(DS1302Read(start_addr));
 202   2                      start_addr+=2;
 203   2              }
 204   1      }
 205          
 206          void numberSend(uchar dat){
 207   1              uchar c1 = dat / 10;
 208   1              uchar c2 = dat % 10;
 209   1              SendData(c1 + '0');
 210   1              SendData(c2 + '0');
 211   1      }
 212          //让时间显示在LCD上，显示小时，分钟，秒
 213          void timeDisplay_HMS(){
 214   1              //读取TIME_AR0-2并显示 
 215   1              uchar TIME_MSG_LINE1[16] = "CURRENT TIME:";
 216   1              timeGetAndStore();
 217   1              setPointer(1,0);
 218   1              SendString(TIME_MSG_LINE1);
 219   1              setPointer(2,0);
 220   1              SendString("TIME:");
 221   1              numberSend(TIME_AR[2]);
 222   1              SendData(':');
 223   1              numberSend(TIME_AR[1]);
 224   1              SendData(':');
 225   1              numberSend(TIME_AR[0]);
 226   1              SendData(' ');
 227   1      }
 228          
 229          //时间显示，年，月，日，周
 230          void timeDisplay_YMD(){
 231   1              uchar TIME_MSG_LINE1[16];
 232   1              timeGetAndStore();
 233   1              sprintf(TIME_MSG_LINE1,"EASY CALENDAR");
 234   1              setPointer(1,0);
 235   1              SendString(TIME_MSG_LINE1);
 236   1              setPointer(2,0);
 237   1              SendString("TIME:");
 238   1              numberSend(TIME_AR[6]);
 239   1              SendData('-');
 240   1              numberSend(TIME_AR[4]);
C51 COMPILER V9.54   CLOCK                                                                 04/10/2022 19:02:40 PAGE 5   

 241   1              SendData('-');
 242   1              numberSend(TIME_AR[3]);
 243   1              SendData(' ');
 244   1      }
 245          
 246          //按键部分。S2用于切换两种显示，S3记录时间。
 247          void statesAlter(){
 248   1              if(!S2){
 249   2                      delay(10);
 250   2                      if(!S2){
 251   3                              switch(S){
 252   4                                      case CLOCK:S = Y_M_D;break;
 253   4                                      case Y_M_D:S = CLOCK;break;
 254   4                              }
 255   3                      
 256   3                      }
 257   2                      while(!S2); //松手检测
 258   2              }
 259   1              if(!S3){
 260   2                      delay(10);
 261   2                      if(!S3)
 262   2                      {
 263   3                              sendTimeToSeries();
 264   3                              //按下S3，把当前时间发送到串口。
 265   3                              while(!S3);
 266   3                      }
 267   2              }
 268   1      }
 269          
 270          void timerInit(){
 271   1              TR1 = 1; //定时器1工作。
 272   1              TMOD |= 0x20;
 273   1              TH1 = 0xfd;
 274   1              TL1 = 0xfd; //波特率9600
 275   1              SM0 = 0;
 276   1              SM1 = 1; //串口选择工作模式
 277   1      }
 278          
 279          void sendTimeToSeries()
 280          {
 281   1              /*
 282   1              串口发送
 283   1              */
 284   1      }
 285          
 286          void main()
 287          {
 288   1              uint cnt = 0;
 289   1              lcdInit();
 290   1              DS1302Init();
 291   1              timerInit();
 292   1              writeOpen(0);
 293   1              while(1){
 294   2                      cnt++;
 295   2                      if(cnt >= 3000)
 296   2                      {
 297   3                              writeOpen(0);
 298   3                              switch(S)
 299   3                              {
 300   4                                      case CLOCK: timeDisplay_HMS();break;
 301   4                                      case Y_M_D: timeDisplay_YMD();break;
 302   4                              }
C51 COMPILER V9.54   CLOCK                                                                 04/10/2022 19:02:40 PAGE 6   

 303   3                              cnt = 0;
 304   3                              writeOpen(1);
 305   3                      }
 306   2                      statesAlter();
 307   2              }
 308   1      }


MODULE INFORMATION:   STATIC OVERLAYABLE
   CODE SIZE        =    695    ----
   CONSTANT SIZE    =     36    ----
   XDATA SIZE       =   ----    ----
   PDATA SIZE       =   ----    ----
   DATA SIZE        =      8      37
   IDATA SIZE       =   ----    ----
   BIT SIZE         =   ----    ----
END OF MODULE INFORMATION.


C51 COMPILATION COMPLETE.  0 WARNING(S),  0 ERROR(S)
